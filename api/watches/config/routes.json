{
  "routes": [
    {
      "method": "GET",
      "path": "/watches",
      "handler": "watches.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/watches/count",
      "handler": "watches.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/watches/:id",
      "handler": "watches.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/watches",
      "handler": "watches.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/watches/:id",
      "handler": "watches.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/watches/:id",
      "handler": "watches.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
